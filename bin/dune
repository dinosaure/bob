(executable
 (name cat)
 (public_name bob.cat)
 (package bob)
 (modules cat)
 (libraries fmt.tty logs.fmt bob.fiber))

(executable
 (name getline)
 (public_name bob.getline)
 (package bob)
 (modules getline)
 (libraries fmt.tty logs.fmt bob.fiber))

(executable
 (name compress)
 (public_name bob.compress)
 (package bob)
 (modules compress)
 (libraries bob.stream))

(library
 (name args)
 (public_name bob.args)
 (modules args)
 (libraries
  cmdliner
  fmt
  logs.cli
  ipaddr
  ipaddr.unix
  base64
  bob.password
  bob.temp
  bob.happy-eyeballs
  ca-certs
  tls
  bob.dns
  fmt.cli
  fmt.tty))

(executable
 (name bob)
 (public_name bob)
 (package bob)
 (modules transfer daemon bob clear bob_clear recv send relay connect)
 (libraries
  ca-certs
  progress
  base64
  ipaddr
  ipaddr.unix
  cmdliner
  bob.fpath
  fmt.cli
  logs.cli
  fmt.tty
  logs.fmt
  bob.args
  bob.password
  bob.unix
  bob.pack
  bob.tls
  bob.dns
  bob.reporters
  bob.extract
  bob.generate
  bob.happy-eyeballs))

(executable
 (name talk)
 (public_name bob.talk)
 (package bob)
 (modules talk)
 (libraries
  cmdliner
  bob.args
  bob.std
  bob.fiber
  bob.protocol
  bob.unix
  bob.happy-eyeballs
  bob))

; (rule
;  (target cat.com)
;  (enabled_if
;   (= %{context_name} esperanto))
;  (mode promote)
;  (deps cat.exe)
;  (action
;   (run objcopy -S -O binary %{deps} %{target})))
; 
; (rule
;  (target getline.com)
;  (enabled_if
;   (= %{context_name} esperanto))
;  (mode promote)
;  (deps getline.exe)
;  (action
;   (run objcopy -S -O binary %{deps} %{target})))
